/**
 *
 * Merge the second array into the first array in ascending order
 * The two arrays with each array containing values from small to large values
 * Both arrays cannot be empty at the same time but one of them may be empty
 * Do not return anything
 */

/**
 * Example:
 * nums1=[1,2,2,3,0,0,0], nums2=[2,5,6]
 *            j                    i
 * nums1=[1,2,2,3,5,6]
 */

var merge = function (nums1, m, nums2, n) {
  
}

console.log(merge([1, 2, 3], 3, [2, 5, 6], 3));
